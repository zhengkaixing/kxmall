<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxmall.user.mapper.KxUserTaskFinishMapper">

    <resultMap id="BaseResultMap" type="com.kxmall.user.domain.KxUserTaskFinish">
        <id column="id" property="id" />
        <result column="task_id" property="taskId" />
        <result column="uid" property="uid" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, uid, status, create_time
    </sql>

    <select id="countByTaskIdsAndUid" resultType="java.lang.Long">
        SELECT COUNT(*) FROM kx_user_task_finish
        WHERE task_id IN
        <foreach collection="taskIds" item="taskId" open="(" separator="," close=")">
            #{taskId}
        </foreach>
        AND uid = #{uid}
    </select>

</mapper>

